name: Apply final Home review fixes

on:
  push:
    branches:
      - refactor/home-experience
    paths:
      - .github/workflows/apply-final-home-review-fixes.yml
      - .github/final-home-review-trigger

permissions:
  contents: write

concurrency:
  group: final-home-review-fixes
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: refactor/home-experience
          fetch-depth: 0

      - name: Apply remaining review fixes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("app/src/main/java/com/luc4n3x/levyra/ui/LevyraApp.kt")
          text = path.read_text(encoding="utf-8")

          def replace_once(old: str, new: str, label: str) -> None:
              global text
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected 1 occurrence, found {count}")
              text = text.replace(old, new, 1)

          def replace_in_function(start_marker: str, end_marker: str, old: str, new: str, label: str) -> None:
              global text
              start = text.find(start_marker)
              end = text.find(end_marker, start + len(start_marker))
              if start < 0 or end < 0:
                  raise SystemExit(f"{label}: function boundaries not found")
              block = text[start:end]
              count = block.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected 1 occurrence, found {count}")
              block = block.replace(old, new, 1)
              text = text[:start] + block + text[end:]

          # Remove the snap behavior that leaves a clipped-looking edge on the mood row.
          text = text.replace(
              "import androidx.compose.foundation.gestures.snapping.rememberSnapFlingBehavior\n",
              "",
              1,
          )

          semantics_anchor = "import androidx.compose.ui.semantics.semantics\n"
          if "import androidx.compose.ui.semantics.contentDescription\n" not in text:
              replace_once(
                  semantics_anchor,
                  semantics_anchor + "import androidx.compose.ui.semantics.contentDescription\n",
                  "semantics import",
              )

          replace_once(
              '''            item(key = "home-top", contentType = "home-header") {
                HomeSectionInset {
                    Column(verticalArrangement = Arrangement.spacedBy(16.dp)) {
                        GreetingBar(
                            userName = state.userName,
                            isResolving = state.isResolving,
                            onSearch = viewModel::openSearch,
                            onSettings = viewModel::openSettings
                        )
                        MoodRow(
                            moods = state.moods,
                            selectedId = state.selectedMood?.id,
                            onSelect = viewModel::selectMood
                        )
                    }
                }
            }
''',
              '''            item(key = "home-top", contentType = "home-header") {
                Column(verticalArrangement = Arrangement.spacedBy(16.dp)) {
                    HomeSectionInset {
                        GreetingBar(
                            userName = state.userName,
                            isResolving = state.isResolving,
                            onSearch = viewModel::openSearch,
                            onSettings = viewModel::openSettings
                        )
                    }
                    MoodRow(
                        moods = state.moods,
                        selectedId = state.selectedMood?.id,
                        onSelect = viewModel::selectMood
                    )
                }
            }
''',
              "Home header and mood row layout",
          )

          replace_once(
              '''                modifier = Modifier
                    .height(36.dp)
                    .clip(shape)
''',
              '''                modifier = Modifier
                    .sizeIn(minHeight = LevyraPlayerDesign.MinimumTouchTarget)
                    .height(36.dp)
                    .clip(shape)
''',
              "Play all touch target",
          )

          replace_once(
              "            .height(LevyraHomeDesign.HeroHeight)\n",
              "            .heightIn(min = LevyraHomeDesign.HeroHeight)\n",
              "Hero minimum height",
          )

          replace_in_function(
              "private fun HomeEditorialCollectionCard(",
              "private fun CollectionArtworkMosaic(",
              "    val shape = RoundedCornerShape(20.dp)\n",
              "    val shape = LevyraHomeDesign.ShelfShape\n",
              "Editorial collection shape",
          )
          replace_in_function(
              "private fun HomeQuickPickRow(",
              "private fun HomeAlbumLoadingRow(",
              "    val shape = RoundedCornerShape(15.dp)\n",
              "    val shape = LevyraHomeDesign.ShelfShape\n",
              "Quick pick shape",
          )

          replace_once(
              '''            .border(Dp.Hairline, border, shape)
            .pressable(onClick = onClick),
        contentAlignment = Alignment.Center
''',
              '''            .border(Dp.Hairline, border, shape)
            .pressable(onClick = onClick)
            .semantics { this.contentDescription = contentDescription },
        contentAlignment = Alignment.Center
''',
              "Header button semantics",
          )
          replace_once(
              '''            Icon(
                imageVector = icon,
                contentDescription = contentDescription,
''',
              '''            Icon(
                imageVector = icon,
                contentDescription = null,
''',
              "Header icon duplicate description",
          )

          mood_start = text.find("@Composable\nprivate fun MoodRow(")
          mood_end = text.find("\n@Composable\nprivate fun SectionHeaderAction", mood_start)
          if mood_start < 0 or mood_end < 0:
              raise SystemExit("MoodRow boundaries not found")

          new_mood = '''@Composable
private fun MoodRow(moods: List<Mood>, selectedId: String?, onSelect: (Mood) -> Unit) {
    LazyRow(
        modifier = Modifier
            .fillMaxWidth()
            .padding(bottom = 4.dp),
        horizontalArrangement = Arrangement.spacedBy(8.dp),
        contentPadding = PaddingValues(
            start = HomeHorizontalInset,
            end = HomeHorizontalInset + 24.dp
        )
    ) {
        items(
            items = moods,
            key = { it.id },
            contentType = { "home-mood" }
        ) { mood ->
            val selected = mood.id == selectedId
            val shape = LevyraHomeDesign.MoodChipShape
            val background = when {
                selected -> Brush.linearGradient(
                    listOf(
                        LevyraCyan.copy(alpha = 0.94f),
                        LevyraViolet.copy(alpha = 0.88f)
                    )
                )
                LevyraIsLight -> SolidColor(Color.White.copy(alpha = 0.76f))
                else -> SolidColor(Color(0xFF12141A).copy(alpha = 0.78f))
            }
            val border = when {
                selected -> Color.White.copy(alpha = 0.16f)
                LevyraIsLight -> Color(0x1711131F)
                else -> Color.White.copy(alpha = 0.07f)
            }

            Box(
                modifier = Modifier
                    .height(LevyraHomeDesign.MoodChipHeight)
                    .clip(shape)
                    .background(background, shape)
                    .border(Dp.Hairline, border, shape)
                    .pressable(onClick = { onSelect(mood) }),
                contentAlignment = Alignment.Center
            ) {
                Text(
                    text = mood.title,
                    color = if (selected) Color.White else LevyraText.copy(alpha = 0.90f),
                    fontSize = 13.sp,
                    lineHeight = 15.sp,
                    fontWeight = if (selected) FontWeight.Bold else FontWeight.SemiBold,
                    maxLines = 1,
                    modifier = Modifier.padding(horizontal = 14.dp)
                )
            }
        }
    }
}
'''
          text = text[:mood_start] + new_mood + text[mood_end:]

          required = (
              "onSearch = viewModel::openSearch",
              ".sizeIn(minHeight = LevyraPlayerDesign.MinimumTouchTarget)",
              ".heightIn(min = LevyraHomeDesign.HeroHeight)",
              ".semantics { this.contentDescription = contentDescription }",
              "val shape = LevyraHomeDesign.MoodChipShape",
              "end = HomeHorizontalInset + 24.dp",
          )
          for marker in required:
              if marker not in text:
                  raise SystemExit(f"Missing required marker: {marker}")
          if "rememberSnapFlingBehavior" in text:
              raise SystemExit("Snap fling behavior still present")

          path.write_text(text, encoding="utf-8")
          PY

          rm -f .github/workflows/apply-final-home-review-fixes.yml .github/final-home-review-trigger

      - name: Commit fixes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(home): clear remaining review feedback"
          git push origin HEAD:refactor/home-experience
