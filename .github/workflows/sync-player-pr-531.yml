name: Sync player PR 531

on:
  push:
    branches:
      - automation/sync-player-pr-531

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe runner
        run: echo ready
