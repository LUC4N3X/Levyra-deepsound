# Levyra Claude Code

@.agents/claude/CLAUDE.md

The tracked Claude Code configuration lives under `.agents/claude/`. If the local native runtime projection is missing or stale, run `python scripts/sync_agent_runtime.py --runtime claude` and restart Claude Code so project settings, hooks, agents, rules, and skill bridges are refreshed.
