# One plugin identifier per line. Installation is always opt-in through the
# Levyra setup scripts; this manifest does not grant repository permissions.
superpowers@openai-curated
codex-security@openai-curated
